#include <stdio.h>
#include <stdlib.h>
#include "sort.c"

int main()
{

int arr[] = {1, 2, 3, 4, 3, 2, 4, 5, 6, 75, 3, 2, 34, 5, 6, 7,3 ,43, 4 3,4};
int min = arr[0], max = arr[0];


for (int i = 1; i < n; i++)
	{
 		if (min > v[i])
		{   		
		min = v[i];
		}
 		if (max < v[i])
		{
   		max = v[i];
		}
	}

cs(v, min, max);

exit (EXIT_SUCSESS);

}
